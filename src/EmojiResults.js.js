"import React from 'react';\nimport { render, cleanup } from '@testing-library/react';\nimport EmojiResults from './EmojiResults';\n\ndescribe('EmojiResults', () => {\n  afterEach(cleanup);\n\n  test('renders without crashing', () => {\n    const { container } = render(<EmojiResults emojiData={[]} />);\n    expect(container).toBeTruthy();\n  });\n\n  test('renders correct number of EmojiResultRow components', () => {\n    const emojiData = [{ title: 'smile', symbol: '😀' }, { title: 'laugh', symbol: '😂' }];\n    const { getAllByTestId } = render(<EmojiResults emojiData={emojiData} />);\n    const emojiRows = getAllByTestId('emoji-row');\n    expect(emojiRows.length).toBe(emojiData.length);\n  });\n\n  test('renders correct emoji symbol and title', () => {\n    const emojiData = [{ title: 'smile', symbol: '😀' }];\n    const { getByText } = render(<EmojiResults emojiData={emojiData} />);\n    expect(getByText('smile')).toBeTruthy();\n    expect(getByText('😀')).toBeTruthy();\n  });\n\n  test('does not render EmojiResultRow component if emojiData is empty', () => {\n    const { queryByTestId } = render(<EmojiResults emojiData={[]} />);\n    const emojiRow = queryByTestId('emoji-row');\n    expect(emojiRow).toBeNull();\n  });\n});\n"