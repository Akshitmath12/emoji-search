"import React from 'react';\nimport { render } from '@testing-library/react';\nimport EmojiResultsRow from '../EmojiResultRow';\n\ndescribe('EmojiResultsRow', () => {\n  let defaultProps;\n\n  beforeEach(() => {\n    defaultProps = {\n      title: 'smiley',\n      symbol: '😀'\n    };\n  });\n\n  const renderComponent = props => render(<EmojiResultsRow {...defaultProps} {...props} />);\n\n  test('should render without crashing', () => {\n    const { getByTestId } = renderComponent();\n    expect(getByTestId('component-emoji-result-row')).toBeInTheDocument();\n  });\n\n  test('should display correct title', () => {\n    const { getByText } = renderComponent();\n    expect(getByText(defaultProps.title)).toBeInTheDocument();\n  });\n\n  test('should display correct info', () => {\n    const { getByText } = renderComponent();\n    expect(getByText('Click to copy emoji')).toBeInTheDocument();\n  });\n\n  test('should display correct emoji', () => {\n    const { container } = renderComponent();\n    const img = container.querySelector('img');\n    expect(img.src).toContain(defaultProps.symbol.codePointAt(0).toString(16));\n    expect(img.alt).toBe(defaultProps.title);\n  });\n\n  test('should have correct clipboard text', () => {\n    const { container } = renderComponent();\n    const div = container.querySelector('div');\n    expect(div.getAttribute('data-clipboard-text')).toBe(defaultProps.symbol);\n  });\n});\n"