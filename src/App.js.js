"import React from \"react\";\nimport { render, fireEvent } from \"@testing-library/react\";\nimport App from \"../App\";\nimport filterEmoji from \"../filterEmoji\";\n\njest.mock(\"../filterEmoji\", () => jest.fn());\n\ndescribe(\"App component\", () => {\n\n  beforeEach(() => {\n    filterEmoji.mockClear();\n  });\n\n  test(\"renders without crashing\", () => {\n    const { getByTestId } = render(<App />);\n    expect(getByTestId(\"app\")).toBeInTheDocument();\n  });\n\n  test(\"initializes with the default emoji list\", () => {\n    render(<App />);\n    expect(filterEmoji).toHaveBeenCalledWith(\"\", 20);\n  });\n\n  test(\"filters emoji list when search input changes\", () => {\n    const { getByPlaceholderText } = render(<App />);\n    const searchInput = getByPlaceholderText(\"Search...\");\n\n    fireEvent.change(searchInput, { target: { value: \"joy\" } });\n\n    expect(filterEmoji).toHaveBeenCalledWith(\"joy\", 20);\n  });\n});\n"