"import filterEmoji from '../src/filterEmoji';\nimport emojiList from '../src/emojiList.json';\n\ndescribe('filterEmoji', () => {\n  let originalEmojiList;\n\n  beforeAll(() => {\n    originalEmojiList = [...emojiList];\n  });\n\n  afterEach(() => {\n    emojiList = [...originalEmojiList];\n  });\n\n  test('returns an array of emojis', () => {\n    const searchText = 'happy';\n    const maxResults = 5;\n    const result = filterEmoji(searchText, maxResults);\n\n    expect(result).toHaveLength(maxResults);\n    result.forEach(emoji => {\n      expect(emoji.title.toLowerCase()).toContain(searchText);\n    });\n  });\n\n  test('returns an empty array if no match found', () => {\n    const searchText = 'nonexistentemoji';\n    const maxResults = 5;\n    const result = filterEmoji(searchText, maxResults);\n\n    expect(result).toHaveLength(0);\n  });\n\n  test('returns all matching emojis if maxResults is not defined', () => {\n    const searchText = 'happy';\n    const result = filterEmoji(searchText);\n\n    expect(result).toEqual(\n      emojiList.filter(emoji => emoji.title.toLowerCase().includes(searchText))\n    );\n  });\n});\n"