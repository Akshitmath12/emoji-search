"import React from 'react';\nimport { render } from '@testing-library/react';\nimport Header from './Header';\n\ndescribe('Header Component', () => {\n  let component;\n\n  beforeEach(() => {\n    component = render(<Header />);\n  });\n\n  test('renders without crashing', () => {\n    expect(component).toBeTruthy();\n  });\n\n  test('renders header with correct class name', () => {\n    const { container } = component;\n    const headerElement = container.querySelector('.component-header');\n    expect(headerElement).toBeTruthy();\n  });\n\n  test('renders correct number of images', () => {\n    const { container } = component;\n    const images = container.querySelectorAll('img');\n    expect(images.length).toBe(2);\n  });\n\n  test('renders correct images', () => {\n    const { container } = component;\n    const images = container.querySelectorAll('img');\n\n    expect(images[0].src).toBe('http://cdn.jsdelivr.net/emojione/assets/png/1f638.png');\n    expect(images[1].src).toBe('http://cdn.jsdelivr.net/emojione/assets/png/1f63a.png');\n  });\n\n  test('renders correct text', () => {\n    const { getByText } = component;\n    const linkElement = getByText(/Emoji Search/i);\n    expect(linkElement).toBeInTheDocument();\n  });\n});\n"